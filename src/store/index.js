import Vue from "vue";
import Vuex from "vuex";

import * as contact from "@/store/modules/contact.js";

Vue.use(Vuex);

export default new Vuex.Store({
  state: {},
  mutations: {},
  actions: {},
  modules: {
    contact
  }
});
